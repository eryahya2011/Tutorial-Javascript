const name = "yahya"
const repoCount = 50

// console.log(name + repoCount + " Value"); --> Outdated 

console.log(`Hello my name is ${name} and my repo count is ${repoCount}`);  // --> More better and easy to read. 

const gameName = new String('Yahya-J');

console.log(gameName[2]);
console.log(gameName.__proto__)

  
console.log(gameName.length);
console.log(gameName.toUpperCase());
console.log(gameName.charAt(2));   // To find the letter at any place say 2 so in Yahya its h.
console.log(gameName.indexOf('h'));   // To find number of any letter say h in Yahya which is 2. 

const newString = gameName.substring(0, 4)  // Last number (4) not included. Neglects -ve values.
console.log(newString);

const anotherString = gameName.slice(-5, 4); // -ve values can be used here, by giving so it will start from behind.
console.log(anotherString);

const newStringOne = "    yahya    "
console.log(newStringOne);
console.log(newStringOne.trim());

const url = "https://yahya.com/yahya%20jafri"  // When making a url if space is given it automatically fills space like by giving %20 in betweeen.
 
console.log(url.replace('%20', '-')); // To remove %20 we wrote this code; the first value(%20) is writen is the place where the removal thing to be placed; and in other (-) is there which means we want to replace %20 with - .
 
console.log(url.includes('sundar'));
console.log(url.includes('yahya'));

console.log(gameName.split('-'));

